"""MCP Stdio Bridge - connects Claude Desktop to CIDX server.

This module provides a bridge between Claude Desktop (which communicates via
JSON-RPC over stdio) and the CIDX server (which provides HTTP/REST API with
MCP endpoints).
"""

__version__ = "1.0.0"
