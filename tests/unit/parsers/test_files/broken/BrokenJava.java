// Intentionally broken Java file to test fallback chunking
package com.example.broken

import java.util.*;

// Missing class keyword and malformed syntax
public BrokenClass {
    this is not valid Java syntax at all
    random words that make no sense
    
    // Incomplete method declaration
    public void someMethod(
        // Missing closing parenthesis and implementation
    
    // Unmatched braces and brackets
    if (condition {
        System.out.println("This will never compile");
    ]
    
    // Invalid variable declarations
    int x = "this is a string but should be int";
    String = 42;
    
    // Malformed generics
    List<> brokenList = new ArrayList<String, Integer>();
    
    // Invalid annotations
    @ThisAnnotationDoesntExist(value = )
    public void anotherMethod() {
        // Unclosed string literals
        String broken = "This string is never closed
        
        // Invalid lambda syntax (wrong context)
        () -> { return "broken lambda"; }
    }
    
    // Missing closing brace for class